{
  "id": "openclaw-mem0",
  "name": "Mem0 Memory",
  "description": "Persistent conversational memory via Mem0. Auto-captures context after each agent turn and auto-recalls relevant memories before responding.",
  "kind": "memory",
  "version": "0.2.0",
  "configSchema": {
    "type": "object",
    "additionalProperties": false,
    "properties": {
      "apiKey": {
        "type": "string",
        "description": "Mem0 API key for authenticating with the memory service."
      },
      "host": {
        "type": "string",
        "description": "Mem0 API host URL. Defaults to https://api.mem0.ai — set this to your self-hosted instance URL."
      },
      "userId": {
        "type": "string",
        "description": "User ID for scoping memories. Defaults to \"default\". Use a unique ID per org or instance to isolate memories."
      },
      "autoCapture": {
        "type": "boolean",
        "description": "Automatically store conversation context after each agent turn. Defaults to true."
      },
      "autoRecall": {
        "type": "boolean",
        "description": "Automatically inject relevant memories into context before each agent turn. Defaults to true."
      }
    },
    "required": ["apiKey", "host"]
  },
  "uiHints": {
    "apiKey": {
      "label": "Mem0 API Key",
      "sensitive": true,
      "placeholder": "m0-...",
      "help": "API key for the Mem0 service. Supports ${MEM0_API_KEY} env var syntax."
    },
    "host": {
      "label": "Mem0 API Host",
      "placeholder": "https://mem.cxcoast.com",
      "help": "Your self-hosted Mem0 API URL."
    },
    "userId": {
      "label": "User ID",
      "placeholder": "default",
      "help": "Scopes memories to this user. Use org-slug or instance-name for isolation."
    },
    "autoCapture": {
      "label": "Auto-Capture",
      "help": "Automatically store conversation context after each agent turn."
    },
    "autoRecall": {
      "label": "Auto-Recall",
      "help": "Automatically inject relevant memories before each agent turn."
    }
  }
}
